# T9 — Bulk MCP register + vertical-slice smoke

**Date:** 2026-08-19  
**Owner:** Developer 2  
**Status:** Done (mock-mode scripts, exit 0)

## What shipped

Scale **pipeline**, not 900 live OAuth apps.

- `backend/scripts/fixtures/mcp_connectors.json` — 12 connector definitions
- `backend/scripts/bulk_register_mcp.py` — loads JSON into `MCPService.register`
- `backend/scripts/smoke_vertical_slice.py` — Nango connect + MCP + Telegram/AstrBot + memory + Harness run
- 3-step add-connector documented in `docs/mcp-catalog.md`
- **No new FastAPI business routers**

## How we verified

```powershell
cd backend
$env:PYTHONPATH = (Get-Location).Path
python scripts/bulk_register_mcp.py
python scripts/smoke_vertical_slice.py
pytest -q
```

| Check | Result |
|-------|--------|
| bulk register | **added 12 connectors without new routers** (catalogSize 13 incl. seed) |
| vertical slice | **VERTICAL SLICE ok** |
| pytest | **37 passed** |

## Demo

[t9-smoke.html](assets/t9-smoke.html) · [t9-smoke.txt](assets/t9-smoke.txt)

```
added 12 connectors without new routers
VERTICAL SLICE ok
nango: mock http://localhost:3003/oauth/connect/github
telegram: Telegram connected (mock)
memory: recalled=True
run: queued mock
```

## Day-5 DoD (Dev2-owned)

| Item | Status |
|------|--------|
| Nango-backed connect (mock/live URL) | T5 |
| AstrBot messaging connector | T8 |
| MCP registry row + invoke (no secrets) | T6 |
| Memory store/recall | T4 |
| AgentService → Harness + ContextBuilder | T7 |
| Compose sidecars | T-stack |
| Routers do not import sidecar adapters | tests |
| Bulk MCP JSON pipeline | **T9** |

Postgres catalog/goals/overview remain Dev1.
